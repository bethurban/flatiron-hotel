<h1>Room <%= @room.room_number %></h1>
<h2>$<%= @room.cost %> per night </h2>
<h2>Capacity: <%= @room.capacity %> guests</h2>
<% if @room_img != "" %>
  <%= link_to image_tag("#{@room_img}", alt: "Flatiron Hotel room #{@room.room_number}", class: "img-responsive") %>
  <br>
<% end %>
<%= image_tag @room.picture.thumb.url %>
<br>
<%= link_to "Back to all rooms", rooms_path %>
