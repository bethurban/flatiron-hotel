{
  "name": "flatiron-hotel",
  "engines": {
    "node": "7.10.1"
  },
  "scripts": {
    "build": "cd ./app/assets/javascripts && npm install && npm run build && cd ..",
    "deploy": "cp -a client/build/. public/",
    "postinstall": "npm run build && npm run deploy && echo 'Client built!'"
  }
}
